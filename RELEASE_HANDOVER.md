# راهنمای تحویل — گالری پروانه رزاقی

**نشانی عمومی:** [https://parvanerazaghiart.com](https://parvanerazaghiart.com)  
**نشانی www:** [https://www.parvanerazaghiart.com](https://www.parvanerazaghiart.com)  
**پنل مدیریت:** [https://parvanerazaghiart.com/admin/login](https://parvanerazaghiart.com/admin/login)  
**نسخه:** v1.1.0 (زنده) — پایهٔ تحویل v1.0.0  
**تاریخ تحویل:** ۲۵ اوت ۲۰۲۶  
**به‌روزرسانی زنده:** ۱۶ سپتامبر ۲۰۲۶ (`2ab895e`)

این سند برای خانم پروانه رزاقی و ادمین فنی گالری است. **رمز عبور در این فایل نیست** و فقط خارج از Git / خارج از این راهنما تحویل می‌شود. هر دو حساب با یک رمز اولیهٔ مشترک ساخته شده‌اند؛ پس از ورود اول آن را در مدیر رمز ذخیره کنید.

---

## ۱. ورود به پنل مدیریت

نشانی ورود: [https://parvanerazaghiart.com/admin/login](https://parvanerazaghiart.com/admin/login)

دو حساب مدیر فعال است. در فیلد **Email or phone** یکی از این شماره‌ها را وارد کنید، سپس رمز تحویل‌شده را بزنید و **Sign in**:

| نقش | شمارهٔ ورود |
|---|---|
| ادمین فنی | `09370504588` |
| ادمین گالری | `09192305309` |

پس از ورود موفق به داشبورد `/admin` می‌روید.

نکته‌ها:

- میانبر `/login` هم به همان صفحهٔ ورود می‌رود.
- اگر چند بار رمز را اشتباه وارد کنید، ورود موقتاً قفل می‌شود؛ چند دقیقه صبر کنید و دوباره تلاش کنید.
- برای خروج از دکمهٔ **Sign out** استفاده کنید.
- بازیابی خودکار رمز در این نسخه نیست؛ فراموشی رمز فقط از طریق اپراتور سرور.

---

## ۲. آثار نقاشی — ایجاد، ویرایش، حذف

منوی **Collection** فهرست همهٔ آثار است.

### ایجاد اثر جدید

مسیر: **Collection** → **New artwork**

گالری ابتدا مشخصات را ذخیره می‌کند، سپس تصویر را می‌گیرد.

| فیلد | توضیح |
|---|---|
| Title | عنوان اثر (الزامی) |
| Technique | سبک / تکنیک (مثلاً رنگ روغن روی بوم) |
| Dimensions | ابعاد (مثلاً ۴۰ × ۵۰ سانتی‌متر) |
| Year | سال خلق اثر |
| Category | دسته‌بندی؛ اگر نیست، ابتدا از **Categories** بسازید |
| Status | نمایش عمومی: `PUBLISHED` — پیش‌نویس: `DRAFT` |
| Medium / Collection / Description | اختیاری |

پس از ذخیره، به صفحهٔ همان اثر می‌روید.

در بخش **Media**:

1. **Upload image** را بزنید.
2. فایل **JPEG، PNG یا WebP** با کیفیت مناسب انتخاب کنید.
3. سقف هر فایل **۱۲ مگابایت** است؛ حداکثر ۱۲ تصویر برای هر اثر.
4. نخستین تصویر، تصویر اصلی می‌شود؛ بعداً می‌توانید تصویر اصلی را عوض کنید.

اثر وقتی در `/gallery` دیده می‌شود که وضعیت آن **PUBLISHED** باشد و دست‌کم یک تصویر داشته باشد.

تصویرها روی فضای مشترک سرور می‌مانند و با به‌روزرسانی سایت پاک نمی‌شوند.

### ویرایش اثر

1. از **Collection** روی عنوان اثر بزنید.
2. عنوان، ابعاد، تکنیک، سال، دسته، وضعیت یا توضیحات را عوض کنید و ذخیره کنید.
3. تصویر تازه بارگذاری کنید، ترتیب را عوض کنید، یا تصویر اصلی را تغییر دهید.

### حذف اثر

از صفحهٔ همان اثر، حذف نرم انجام می‌شود (از گالری عمومی برداشته می‌شود). در صورت نیاز، اپراتور می‌تواند اثر حذف‌شده را از فیلتر مربوط در Collection بازیابی کند.

---

## ۳. پیام‌ها و استعلام‌های مشتریان

مسیر: **Enquiries**

بازدیدکنندگان از فرم تماس / استعلام پیام می‌گذارند. در این فهرست می‌بینید:

- نام
- راه تماس (ایمیل یا تلفنی که خودشان نوشته‌اند)
- متن پیام
- اثر مرتبط (اگر از صفحهٔ یک نقاشی فرستاده باشند)

پاسخ از داخل سایت ارسال نمی‌شود. با همان راه تماس، از ایمیل یا تلفن خودتان جواب بدهید.

وضعیت را به‌روز کنید:

- `NEW` — خوانده نشده
- `READ` — دیده شده / در حال پیگیری
- `CLOSED` — بسته شده

---

## ۴. دسته‌بندی‌ها

از **Categories** دسته بسازید، ویرایش کنید یا غیرفعال کنید. دستهٔ فعال در فرم اثر و فیلتر گالری دیده می‌شود.

---

## ۵. امنیت رمز و کش مرورگر

- پس از ورود اول، رمز را در یک برنامهٔ مدیریت رمز ذخیره کنید؛ در پیام یا ایمیل عمومی نفرستید.
- بازیابی خودکار رمز در این نسخه نیست. اگر رمز فراموش شد، به اپراتور استقرار بگویید تا روی سرور بازنشانی شود.
- نشست ورود حدود ۱۵ دقیقه برای دسترسی فعال است؛ در صورت خروج خودکار دوباره وارد شوید.
- اگر صفحهٔ تازه را نمی‌بینید: **Ctrl+F5** یا پنجرهٔ ناشناس.

---

## ۶. آنچه در این نسخه نیست (عمدی)

- فروش آنلاین و درگاه پرداخت
- حساب کاربری برای بازدیدکنندگان
- بازیابی رمز از طریق ایمیل
- اپلیکیشن موبایل جداگانه

برای امضای تحویل از `CUSTOMER_ACCEPTANCE_CHECKLIST.md` استفاده کنید.

---

## ۷. انتشار v1.1.0 (۱۶ سپتامبر ۲۰۲۶)

سایت زنده روی **v1.1.0** (`2ab895e`) است.

- صفحهٔ **About** با زندگینامه، نمایشگاه‌ها و تصویر استودیو
- امضای برند در سربرگ و فیلم آتلیه (پخش فقط با انتخاب بازدیدکننده)
- پاورقی **Powered by TECHOOCH** با پیوند اینستاگرام
- منوی موبایل: Home، Gallery، About، Contact

ورود ادمین، گالری آثار، و پیام‌های استعلام مثل v1.0.0 است. Health عمومی و HTTPS تأیید شد.
